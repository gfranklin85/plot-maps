'use client';

// /globe — preview surface for the GlobeHero prototype (the flyable Earth of
// wonders front door). Eyeball the feel here before committing the real front
// page to it. memory: the front-door breakthrough.
import GlobeHero from '@/components/globe/GlobeHero';

export default function GlobePreviewPage() {
  return <GlobeHero />;
}
