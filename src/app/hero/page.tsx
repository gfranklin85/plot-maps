'use client';

// /hero — preview of the cinematic video hero (Greg's locked front-page
// mockup). Eyeball here before swapping it in as the real front page.
import CinematicHero from '@/components/landing/CinematicHero';

export default function HeroPreviewPage() {
  return <CinematicHero />;
}
